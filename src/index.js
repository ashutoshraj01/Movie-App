import React from 'react';
import ReactDOM from 'react-dom'; // helps to render out the app in the browser.It has method for rendering in the dom.
import App from './components/App/App';
import './index.css';

ReactDOM.render(<App/>,document.getElementById('root')); //render methods takes two arguments - what we want to render and where we want to render


